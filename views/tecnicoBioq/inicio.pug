.d-none
  include navBar.pug

include listaOrdenes.pug
   
  script.   
       const modal1=("#{modal}"!=="false")
       if(modal1){
       const miModal = new bootstrap.Modal(document.getElementById('miModal'));
         miModal.show();
         }    